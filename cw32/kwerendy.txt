kw1
SELECT nazwa, ulica FROM `kwiaciarnie` WHERE miasto="Malbork";
kw2
SELECT id_kwiaciarni, COUNT(*) AS liczba_zamowien FROM `zamowienia` GROUP BY id_kwiaciarni; 
kw3
SELECT nazwa, miasto, kwiaty FROM `kwiaciarnie` JOIN `zamowienia` ON kwiaciarnie.id_kwiaciarni=zamowienia.id_kwiaciarni WHERE DATA='2017-01-07';
kw4
CREATE TABLE `cw32`.`klienci` (`id` INT NOT NULL AUTO_INCREMENT , `imie` VARCHAR(15) NOT NULL , `nazwisko` VARCHAR(15) NOT NULL , `rabat` TINYINT UNSIGNED NOT NULL  , PRIMARY KEY (`id`)) ENGINE = InnoDB;